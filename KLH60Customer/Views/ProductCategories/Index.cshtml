@model IEnumerable<ProductCategory>

@{
    ViewData["Title"] = "Product Categories";
}

<h1>Viewing All Categories</h1>
<h3>Product Categories</h3>
<p>
    @foreach (var item in Model)
    {
    <span>     |     <a asp-action="ProductsInCategory" asp-controller="Products" asp-route-catId="@item.CategoryId">@item.ProdCat</a>     |     </span>
    }
</p>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProdCat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ProdCat)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CategoryId">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.CategoryId">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.CategoryId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>*@